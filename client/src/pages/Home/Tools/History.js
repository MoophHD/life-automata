import React from "react";
import styled from "styled-components";

const History = () => (<Container>history</Container>);

const Container = styled.div`
color: white;
  flex: 1;
`;

export default History;
